# 2025-07-30, 김병현 수정 - 백엔드 개발환경용 Dockerfile
FROM node:20-alpine

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 package-lock.json 복사하여 의존성 설치
COPY package*.json ./
RUN npm install

# 포트 3000 노출 (NestJS 기본 포트)
EXPOSE 3000

# 개발 서버 시작 (watch 모드)
CMD ["npm", "run", "start:dev"]